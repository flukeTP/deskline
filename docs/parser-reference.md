# Parser porting notes

Deskline MVP shell uses `StubQuotaProvider`. Real parsers will be adapted from:

```text
~/Documents/project/public/ai-usage-counter
```

See `docs/decisions.md` for the file map per provider.
